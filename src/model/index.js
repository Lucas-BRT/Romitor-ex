import * as localdata from "./local";
import * as metadata from "./room";

const VERSION = "1.0.0";
const METADATA_PATH = "com.romitor-ex/metadata";
const DEFAULT_METADATA = {
  players: [],
};
export { localdata, metadata, VERSION, METADATA_PATH, DEFAULT_METADATA };
